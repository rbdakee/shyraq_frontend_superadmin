# Shyraq SuperAdmin

Внутренний веб-инструмент команды Shyraq для управления SaaS-платформой детских садов: тенанты, подписки, фичефлаги, операторские триггеры, DLQ. Аудитория — 5–20 internal users за VPN/IP-allowlist. Desktop-first (≥ 768px), light theme.

## Setup

1. `pnpm install`
2. `cp .env.example .env.local` — fill in `VITE_API_BASE_URL` (default `/api/v1`, served via Vite dev proxy to the live dev backend).
3. `pnpm dev` — Vite dev server on `http://localhost:5173`. The backend configured in `BACKEND_ORIGIN` is proxied automatically.

## Scripts

| Script           | Description                                     |
| ---------------- | ----------------------------------------------- |
| `pnpm dev`       | Vite dev server                                 |
| `pnpm build`     | Production build → `dist/`                      |
| `pnpm preview`   | Serve the production build locally              |
| `pnpm typecheck` | `tsc --noEmit`                                  |
| `pnpm lint`      | ESLint                                          |
| `pnpm test`      | Vitest unit tests                               |
| `pnpm gen:api`   | Regenerate OpenAPI types from live Swagger JSON |
| `pnpm format`    | Prettier format                                 |

## Backend integration

See [`CLAUDE.md` §3](CLAUDE.md#3-backend-integration). All endpoints under `/api/v1/*` proxied to the live dev backend in development. OpenAPI types are generated by `pnpm gen:api` and committed.

## Architecture overview

SPA on Vite 8 + React 19 + TypeScript (strict). TanStack Query for server state, Zustand for UI state (sidebar / locale / command palette / shortcuts help). shadcn/ui primitives over Tailwind v4. i18next RU + KK across 7 namespaces. In-memory access token + localStorage refresh token (Auth flow per `docs/architecture.md §6`). Route-level `React.lazy` + Suspense; mobile alert below 768px; DIY error tracking with `crypto.randomUUID()` shown on the 500 page. See [`docs/architecture.md`](docs/architecture.md) for the full layering, [`docs/DESIGN.md`](docs/DESIGN.md) for per-page specs, and [`docs/IMPLEMENTATION_PLAN.md`](docs/IMPLEMENTATION_PLAN.md) for the batch history.

## Deploy

**Vercel** (Git integration) + **GitHub Actions** quality gate.

- **CI gate** (`.github/workflows/ci.yml`): on push/PR to `main` runs `typecheck → lint → test → build` (Node 20, pnpm frozen lockfile).
- **Vercel**: framework `vite`, builds `dist/`; Routing Middleware reverse-proxies `/api/*` to `BACKEND_ORIGIN` (same-origin, no CORS, no mixed-content); `vercel.json` provides the SPA fallback, security headers, and cache-control.
- **One-time Vercel setup** (manual): import the GitHub repo, set `VITE_API_BASE_URL=/api/v1` and `BACKEND_ORIGIN=https://your-backend.example.com` in Project → Environment Variables for Production and Preview, production branch = `main`. Routing Middleware proxies `/api/*` to that backend at runtime.
- ⚠️ Network perimeter (IP-allowlist / Vercel Deployment Protection) is **deferred** — see [`docs/OPEN_QUESTIONS.md#a3`](docs/OPEN_QUESTIONS.md). Until then the only gate is app-login.

Full rationale: [`docs/architecture.md §11`](docs/architecture.md) and "Post-B8 — Deployment" in [`docs/IMPLEMENTATION_PLAN.md`](docs/IMPLEMENTATION_PLAN.md).

## License

Internal — no public license.
